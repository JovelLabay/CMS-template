const transactions = [{ id: 1, name: 'Dashboard', path: '/' }];

const operations = [{ id: 1, name: 'Others', path: '/' }];

const settings = [
  { id: 2, name: 'Branches', path: '/' },
  { id: 3, name: 'Users', path: '/' },
];

export { transactions, operations, settings };
